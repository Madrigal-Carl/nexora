import 'package:flutter/material.dart';

class NexisPage extends StatelessWidget {
  const NexisPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Nexis'));
  }
}
